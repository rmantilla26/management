<%= form_for(@project_resource) do |f| %>
  <% if @project_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_resource.errors.count, "error") %> prohibited this project_resource from being saved:</h2>

      <ul>
      <% @project_resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.text_field :project_id %>
  </div>
  <div class="field">
    <%= f.label :resource_id %><br>
    <%= f.text_field :resource_id %>
  </div>
  <div class="field">
    <%= f.label :order %><br>
    <%= f.number_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
